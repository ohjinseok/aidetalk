# Security Policy

이 문서는 **취약점을 발견했을 때 신고하는 절차**를 안내합니다. 프로젝트가 구현해야
하는 보안 요구사항 자체(시크릿 취급, 인증, 레이트리밋 등)는 개발 참고 문서인
[`docs/08_SECURITY.md`](./docs/08_SECURITY.md)에 있으며, 이 문서와는 목적이 다릅니다.

## 지원 버전

AideTalk은 아직 M0(검증 & 기반) 단계이며 정식 버전이 태그되지 않았습니다. 버전 정책이
확정되는 대로(`docs/12_GIT_STRATEGY.md` §3 참고) 아래 표를 갱신합니다.

| 버전 | 보안 패치 지원 |
|---|---|
| `main` (최신 개발 브랜치) | ✅ |
| 최신 릴리스(`v1.x` 중 최신 minor) | ✅ |
| 그 이전 minor 버전 | ❌ (업그레이드 권장) |

셀프호스팅 사용자는 항상 최신 minor 버전으로 업그레이드하는 것을 권장합니다.
클라우드(SaaS) 사용자는 별도 조치 없이 항상 최신 패치가 자동 적용됩니다.

## 취약점 신고 방법

**공개 Issue로 취약점을 신고하지 마세요.** 악용될 수 있는 정보가 공개되기 전에 저희가
먼저 검토할 시간이 필요합니다.

취약점은 **GitHub Security Advisories**를 통해 비공개로 신고해 주세요:

1. https://github.com/aidetalk/aidetalk/security/advisories/new 로 이동
   (GitHub 저장소 상단 **Security 탭 → Report a vulnerability**에서도 접근 가능)
2. 아래 내용을 최대한 구체적으로 포함해 작성:
   - 취약점 종류 (예: 인증 우회, SSRF, XSS, 권한 격리 위반 등)
   - 영향을 받는 컴포넌트/파일 경로 (예: `apps/server/src/...`, 위젯, 특정 API 엔드포인트)
   - 재현 절차 (요청 예시, curl 명령, 스크린샷 등)
   - 영향 범위에 대한 판단 (예: 단일 워크스페이스 vs 전체 인스턴스, 데이터 읽기 vs 쓰기)
   - 가능하다면 제안하는 수정 방향
3. GitHub 계정 없이 신고해야 하는 경우 저장소 owner 프로필의 연락처를 이용해 주세요.

이메일 등 별도 비공개 채널이 아직 준비되지 않은 1인 프로젝트 단계이므로, 현재는
GitHub Security Advisories가 유일한 공식 신고 경로입니다.

## 처리 절차 및 응답 시간 목표

- **최초 응답**: 신고 접수 후 영업일 기준 **3일 이내** 확인 회신.
- **심각도 평가**: 접수 후 **7일 이내** CVSS 기준 심각도 판단 및 대응 계획 공유.
- **패치**: 심각도에 따라 다르지만, Critical/High 등급은 확인 후 **14일 이내** 패치를
  목표로 합니다. 즉시 완화가 필요한 경우 임시 조치(mitigation) 안내를 먼저 제공할 수
  있습니다.
- **공개**: 패치 배포 후 GitHub Security Advisory를 공개하며, 신고자가 원할 경우
  Credit(발견자 표기)을 남깁니다. 조율된 공개(coordinated disclosure)를 원칙으로 하며,
  신고자와 합의 없이 조기 공개하지 않습니다.

## 신고 대상 예시

다음과 같은 이슈는 이 절차를 통해 신고해 주세요:

- 워크스페이스 간 데이터 격리 위반 (한 워크스페이스가 다른 워크스페이스 자원에 접근)
- visitor 권한으로 상담원 전용 자원(assist_suggestions, 인박스 API 등) 접근 가능
- Agent Dispatcher HMAC 검증 우회, SSRF 가드 우회
- 인증/세션 토큰 위조 또는 탈취 가능성
- 의존성(공급망) 취약점 중 실제 공격 경로가 있는 경우
- 시크릿(shared secret, 세션 시크릿 등)이 로그·응답에 노출되는 경우

일반적인 버그, 기능 요청, "이게 취약점인지 모르겠는" 질문은 공개 Issue 또는
Discussions를 이용해 주세요.

## Safe Harbor

신의성실(good faith)에 따라 이 절차를 통해 취약점을 신고하고, 데이터 파괴·서비스 중단·
개인정보 침해를 유발하지 않는 범위에서 조사한 연구자에 대해서는 법적 조치를 취하지
않습니다.
