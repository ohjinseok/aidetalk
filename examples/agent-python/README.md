# examples/agent-python — 예제 Agent 커넥터 (Python / FastAPI)

`examples/agent-node`와 동일한 계약(`05_AGENT_PROTOCOL.md`)의 Python/FastAPI 구현.

## 상태

M0 단계 — 아직 빈 상태(placeholder README만). 실제 구현은 M2(셀프호스팅 패키징) 단계에서 진행한다
(`docs/11_ROADMAP.md` M2 참고).

## 주의

- 이 예제는 `examples/` 하위이므로 LLM 호출 코드가 허용된다 (CLAUDE.md 절대 규칙 1의 예외).
- 코어(`apps/`, `packages/`)는 이 예제를 import하지 않는다.
