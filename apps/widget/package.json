{
  "name": "@aidetalk/widget",
  "version": "0.0.0",
  "private": true,
  "description": "임베드 채팅 위젯 (Preact + Shadow DOM). React 금지, Preact까지만 — 번들 예산 50KB gzip",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aidetalk/i18n": "workspace:*",
    "preact": "^10.29.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^22.10.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  }
}
