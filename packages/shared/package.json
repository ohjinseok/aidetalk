{
  "name": "@aidetalk/shared",
  "version": "0.0.0",
  "private": true,
  "description": "API 계약 단일 출처(zod 스키마) + 공유 타입 — 실제 구현은 이후 세션에서 채운다 (placeholder)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
