{
  "name": "@aidetalk/db",
  "version": "0.0.0",
  "private": true,
  "description": "Drizzle 스키마 + 마이그레이션 + repos — 실제 구현은 이후 세션에서 채운다 (placeholder)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "echo \"TODO(question): drizzle-kit 설정 후 마이그레이션 생성 스크립트로 교체\"",
    "db:migrate": "echo \"TODO(question): drizzle-kit migrate 스크립트로 교체\""
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
